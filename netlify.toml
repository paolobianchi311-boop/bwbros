[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# El QR lleva a /canje (sin .html) → servimos el archivo
[[redirects]]
  from = "/canje"
  to = "/canje.html"
  status = 200
